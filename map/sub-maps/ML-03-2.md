&lt;!-- analysis-version: 0 | commit: a5179f6588dd03cbe83a8d8b718a61875dba7b24 | updated: 2025-06-18 | mode: full | split-from: ML-03 | split-strategy: layer | total-segments: 2 --&gt;
# Sub-Map: ML-03-2 工具系统注册与调度 (Layer 2: 执行引擎 + 结果存储 + Representative 工具)

> Split from ML-03 due to deep trace lines (10,070) exceeding 10,000 limit.
> This segment covers the tool execution engine, result storage, and 5 representative tool instances.

## Entry Point
- File: [`src/services/tools/toolExecution.ts`](/src/src/services/tools/toolExecution.ts)
- Function: `runToolUse()`
- Confirmed: YES

## Files Traced

### Core Files (execution + storage)

| # | File | Role | Lines | Key Symbols |
|---|------|------|-------|-------------|
| 1 | [`src/services/tools/toolExecution.ts`](/src/src/services/tools/toolExecution.ts) | 单工具执行完整生命周期：validateInput → checkPermissions → runPreToolUseHooks → tool.call() → processToolResult → runPostToolUseHooks，含 OTel telemetry 和 error classification | 1745 | `runToolUse()`, `classifyToolError()`, `ruleSourceToOTelSource()`, `decisionReasonToOTelSource()`, `HOOK_TIMING_DISPLAY_THRESHOLD_MS` |
| 2 | [`src/utils/toolResultStorage.ts`](/src/src/utils/toolResultStorage.ts) | 工具结果持久化：大结果 (>100KB) 写临时文件而非内存，preview 截断给模型 | 1040 | `processToolResultBlock()`, `processPreMappedToolResultBlock()`, `storeToolResult()`, `getToolResultPreview()` |

### Representative Tools (deep traced instances of PI-01)

| # | File | Role | Lines | Key Symbols |
|---|------|------|-------|-------------|
| 3 | [`src/tools/BashTool/BashTool.tsx`](/src/src/tools/BashTool/BashTool.tsx) | 最复杂内置工具：shell 执行 + 多层安全检查（path validation + read-only validation + bash permissions + bash security） | 1143 | `BashTool` (buildTool), `call()`, `checkPermissions()`, 安全检查链 |
| 4 | [`src/tools/AgentTool/AgentTool.tsx`](/src/src/tools/AgentTool/AgentTool.tsx) | 子代理工具：递归调用查询引擎执行子任务，支持自定义 prompt 和工具子集 | 1397 | `AgentTool` (buildTool), `call()`, 子代理生命周期管理 |
| 5 | [`src/tools/FileReadTool/FileReadTool.ts`](/src/src/tools/FileReadTool/FileReadTool.ts) | 文件读取工具：支持行范围、多个文件、输出格式化 | 1183 | `FileReadTool` (buildTool), `call()`, `validateInput()` |
| 6 | [`src/tools/MCPTool/MCPTool.ts`](/src/src/tools/MCPTool/MCPTool.ts) | MCP 工具桥接器：极薄代理层，转发 call 到 MCP server connection | 77 | `MCPTool` (buildTool), `call()` |
| 7 | [`src/tools/ToolSearchTool/ToolSearchTool.ts`](/src/src/tools/ToolSearchTool/ToolSearchTool.ts) | 工具搜索工具（自引用）：延迟加载未在当前 tool pool 中的工具 | 471 | `ToolSearchTool` (buildTool), `call()`, tool discovery |

**Layer 2 total: 7 files / 7,056 lines**

## Branch Points (diverge from this main line)

| File:Function | Branches To | Likely Main Line | Notes |
|--------------|-------------|-----------------|-------|
| `toolExecution.ts:runToolUse() → runPreToolUseHooks` | [`src/services/tools/toolHooks.ts`](/src/src/services/tools/toolHooks.ts) | ML-04 (权限系统) | Hook 系统与权限决策交互 |
| `toolExecution.ts:runToolUse() → checkPermissions` | [`src/utils/permissions/permissions.ts`](/src/src/utils/permissions/permissions.ts) | ML-04 | 权限检查核心 |
| `BashTool.tsx:call() → bashPermissions` | [`src/tools/BashTool/bashPermissions.ts`](/src/src/tools/BashTool/bashPermissions.ts) | ML-03 sub-module | Bash 专属安全链（文件内部 import） |
| `AgentTool.tsx:call() → query` | `src/services/api/query.ts` | ML-02 | 子代理递归调用查询引擎 |
| `MCPTool.ts:call() → MCPClient` | [`src/services/mcp/client.ts`](/src/src/services/mcp/client.ts) | ML-05 | MCP 协议通信 |
| `FileReadTool.ts:call() → fs.readFile` | Node.js fs | — | 不追踪到 Node 标准库 |

## Cross-References (shared with other main lines)

| File | This Main Line Role | Other Main Line(s) |
|------|-------------------|-------------------|
| [`src/utils/toolSearch.ts`](/src/src/utils/toolSearch.ts) | 工具搜索逻辑 | ML-03-1 (Layer 1 的 supporting file) |
| [`src/utils/toolResultStorage.ts`](/src/src/utils/toolResultStorage.ts) | 结果持久化 | ML-02 (claude.ts 调用 processToolResultBlock) |

## Pattern Instances (本主线 owns 的批量实例，编目模式)

> 实例数据见 `instance-manifest.jsonl`。本节仅汇总。

| Pattern | Representative | Instance Count | Manifest Notes |
|---------|---------------|---------------|----------------|
| PI-01 tool-instance | [`src/tools/BashTool/BashTool.tsx`](/src/src/tools/BashTool/BashTool.tsx) (1143 lines) | 194 catalog + 5 representative = **199 total** | 覆盖 src/tools/ 全部 199 个 .ts/.tsx 文件 |

### PI-01 Instance Distribution (by tool subdirectory)

| Tool Directory | File Count | Total Lines | Largest File | Notes |
|---------------|-----------|-------------|-------------|-------|
| BashTool/ | 16 | ~12,000 | bashPermissions.ts (2621) | Shell 执行 + 多层安全检查 |
| AgentTool/ | 21 | ~8,500 | AgentTool.tsx (1397) | 子代理，含 UI 组件 |
| PowerShellTool/ | 14 | ~10,200 | pathValidation.ts (2049) | Windows PowerShell 等效 |
| FileEditTool/ | 5 | ~2,100 | utils.ts (775) | 文件编辑工具 |
| FileReadTool/ | 5 | ~2,100 | FileReadTool.ts (1183) | 文件读取工具 |
| LSPTool/ | 6 | ~2,400 | LSPTool.ts (860) | LSP 操作工具 |
| SkillTool/ | 4 | ~1,800 | SkillTool.ts (1108) | 技能工具 |
| WebFetchTool/ | 4 | ~700 | WebFetchTool.ts (318) | HTTP 获取 |
| GrepTool/ | 3 | ~800 | GrepTool.ts (577) | 搜索工具 |
| TodoTool/ | 2 | ~250 | TodoTool.ts (166) | 待办列表 |
| ~30 other tools | ~1-3 each | varies | varies | 各类工具 |

### 防滥用约束检查

| 约束 | 结果 |
|------|------|
| 中位数×2 = ~400 行，无 catalog 文件超过此阈值且未标记 WARNING | ⚠️ 部分 catalog 文件 >400 行（如 SkillTool.ts 1108、spawnMultiAgent.ts 1093），但它们是独立工具入口，结构同构 |
| 最大/最小行数比：777/6 = 129 > 10 | ⚠️ WARNING — MCPTool.ts(77) 与较大工具差距大，但 MCPTool 确实是最简代理层 |
| 同构性：所有 PI-01 文件共享 buildTool() 工厂 + Tool 接口 | ✅ PASS |
| role_one_liner 多样性：每个工具有独立描述 | ✅ PASS |

## Architecture: Tool Execution Pipeline

```
                      runToolUse(tool, input, context)
                              │
                 ┌────────────▼────────────┐
                 │ 1. validateInput(input)  │ → Zod schema validation
                 └────────────┬────────────┘
                              │
                 ┌────────────▼────────────┐
                 │ 2. checkPermissions()    │ → ML-04: 权限系统
                 │    + canUseTool (hook)   │ → 用户交互
                 └────────────┬────────────┘
                              │
                 ┌────────────▼────────────┐
                 │ 3. runPreToolUseHooks()  │ → Hook 系统拦截
                 │    decision: allow/deny  │
                 └────────────┬────────────┘
                              │ (denied → abort)
                 ┌────────────▼────────────┐
                 │ 4. tool.call(input, ctx) │ → 具体工具实现
                 │    [BashTool/AgentTool/  │
                 │     FileReadTool/...]    │
                 └────────────┬────────────┘
                              │
                 ┌────────────▼────────────┐
                 │ 5. processToolResult()   │ → toolResultStorage
                 │    large → disk file     │    (ML-03: 1040 lines)
                 │    small → memory        │
                 └────────────┬────────────┘
                              │
                 ┌────────────▼────────────┐
                 │ 6. runPostToolUseHooks() │ → Hook 后处理
                 │    success / failure     │
                 └────────────┬────────────┘
                              │
                 ┌────────────▼────────────┐
                 │ 7. OTel telemetry        │ → span + events
                 └─────────────────────────┘
```

## Key Insights

1. **`runToolUse()` 是执行心脏** (1745 行)：完整的 7 阶段 pipeline（validate → permission → pre-hooks → execute → result-storage → post-hooks → telemetry），每个阶段都有详细的错误处理和遥测。

2. **双模式执行**: 传统 `runTools()` (分批串行/并行) 和 `StreamingToolExecutor` (边流边执行)，都调用同一个 `runToolUse()` 核心函数。

3. **工具结果大小感知**: `toolResultStorage.ts` 对大结果（>100KB）自动持久化到磁盘，返回截断 preview 给模型，防止 context 爆炸。

4. **BashTool 最复杂** (1143 行)：多文件安全体系（bashPermissions 2621行 + bashSecurity 2592行 + pathValidation 1303行 + readOnlyValidation 1990行），占总工具代码量的最大份额。

5. **AgentTool 递归调用** (1397 行)：子代理本质上是一个新的查询引擎循环（调用 query.ts），可递归嵌套。

6. **MCPTool 极简代理** (77 行)：不做任何处理，纯粹转发到 MCP server，是最薄的工具实现。
