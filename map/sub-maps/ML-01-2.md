&lt;!-- analysis-version: 0 | commit: a5179f6 | updated: 2025-06-30 | mode: full | split-from: ML-01 | split-strategy: stage | total-segments: 3 --&gt;
# Sub-Map: ML-01-2 CLI 启动与命令路由 (Stage 2: 初始化序列)

## Entry Point
- File: [`src/entrypoints/init.ts`](/src/src/entrypoints/init.ts.md)
- Function: `init()`
- Confirmed: YES

## 调用链路（本段）

```
main.tsx preAction hook (ML-01-3)
  → init()                                         [init.ts]
    ├─ enableConfigs()                              [config.ts]
    │   ├─ getGlobalConfig()                        [config.ts]
    │   └─ saveGlobalConfig()                       [config.ts]
    ├─ setupState getters                           [state.ts]
    ├─ env detection (BUN/CODEGEN/CI/NON_INTERACTIVE)
    ├─ authPreCheck → OAuth redirect                [→ ML-06]
    ├─ mTLS/passthrough detection
    ├─ proxy configuration
    ├─ API preconnect check
    └─ initializeTelemetryAfterTrust()
  → initSinks()                                     [sinks.ts]
  → setupWarningHandler()                           [warningHandler.ts]
```

## Files Traced

### Core Files (directly on main line path)
| # | File | Role | Lines | Key Symbols |
|---|------|------|-------|-------------|
| 1 | [`src/entrypoints/init.ts`](/src/src/entrypoints/init.ts.md) | memoized 初始化序列（configs→env→OAuth→mTLS→proxy→API preconnect→telemetry），被 main.tsx preAction hook 调用 | 340 | `init()`, `initializeTelemetryAfterTrust()` |

### Supporting Files (utilities/helpers used by core)
| # | File | Role | Lines | Called By |
|---|------|------|-------|-----------|
| 1 | [`src/utils/config.ts`](/src/src/utils/config.ts.md) | 配置管理核心：enableConfigs/getGlobalConfig/saveGlobalConfig/recordFirstStartTime；读写 ~/.claude/ 目录和项目 .claude/ | 1817 | init.ts, cli.tsx, main.tsx |
| 2 | [`src/bootstrap/state.ts`](/src/src/bootstrap/state.ts.md) | 全局状态单例：~100+ getter/setter 管理 session/cwd/cost/model/permissions/verbose 等全局状态 | 1758 | init.ts, main.tsx, 全局 |
| 3 | [`src/utils/sinks.ts`](/src/src/utils/sinks.ts.md) | 日志 sink 初始化（将 stderr 重定向到 sink） | 16 | main.tsx preAction |
| 4 | [`src/utils/warningHandler.ts`](/src/src/utils/warningHandler.ts.md) | 全局 Node.js warning 捕获与格式化 | 121 | main.tsx preAction |

## Branch Points

无。初始化序列是一条直线执行路径，所有分支（OAuth/mTLS/proxy）都在同一文件内内联处理。

## Cross-References (shared with other main lines)
| File | This Main Line Role | Other Main Line(s) |
|------|-------------------|-------------------|
| [`src/utils/config.ts`](/src/src/utils/config.ts.md) | 配置读写 | ML-02, ML-03, ML-05, ML-06, ML-07, ML-11 |
| [`src/bootstrap/state.ts`](/src/src/bootstrap/state.ts.md) | 全局状态读写 | **ALL MLs** — 几乎所有主线都依赖此单例 |
| [`src/entrypoints/init.ts`](/src/src/entrypoints/init.ts.md) | 认证初始化 | ML-06 (OAuth flow) |

## Branch Lines
无。init.ts 内的 OAuth/mTLS/proxy 分支均为短分支（&lt;50 行），不满足 BL 条件。

## Pattern Instances
本段无 Pattern Catalog。

## Segment Summary
- Total files traced: 5 (core: 1, supporting: 4)
- Total ML lines (DEEP): 4,052
- Branch lines discovered: 0
- Entry → Exit: `main.tsx preAction` → `init()` → configs/state/env/OAuth/proxy/telemetry 初始化 → 返回 control 给 Commander action handler
- Key architectural insight: 初始化采用 memoized 单次执行模式（`init()` 被 main.tsx 的 `preAction` hook 调用，确保在任何 action handler 之前完成）。config.ts (1817行) 和 state.ts (1758行) 是两个巨型共享模块，前者管理配置层（~/.claude/ + .claude/），后者管理运行时状态（~100 个 getter/setter）。这种设计使得任何模块都可以通过 `getState()/setState()` 访问全局状态，但也造成了全局耦合。
