&lt;!-- analysis-version: 0 | commit: a5179f6 | updated: 2025-06-30 | mode: full | split-from: ML-01 | split-strategy: stage | total-segments: 3 --&gt;
# Sub-Map: ML-01-1 CLI 启动与命令路由 (Stage 1: 入口分发)

## Entry Point
- File: [`src/bootstrap-entry.ts`](/src/src/bootstrap-entry.ts.md)
- Function: default export (anonymous)
- Confirmed: YES

## 调用链路（本段）

```
bootstrap-entry.ts (L1-5)
  → bootstrapMacro.ensureBootstrapMacro()         [bootstrapMacro.ts]
  → dynamic import('../entrypoints/cli.js')        [cli.tsx]
      → cliMain()
        ├─ 12+ 快速路径分支 (--version, --dump, --chrome-mcp, bridge, daemon, ps/logs, ...)
        │   → 各分支动态导入目标模块（详见 Branch Points 节）
        └─ 默认 fallthrough
            → dynamic import('../main.js')          [main.tsx — ML-01-2/ML-01-3]
```

## Files Traced

### Core Files (directly on main line path)
| # | File | Role | Lines | Key Symbols |
|---|------|------|-------|-------------|
| 1 | [`src/bootstrap-entry.ts`](/src/src/bootstrap-entry.ts.md) | 绝对入口：ensureBootstrapMacro() → dynamic import cli.tsx | 5 | `default()` |
| 2 | [`src/bootstrapMacro.ts`](/src/src/bootstrapMacro.ts.md) | globalThis.MACRO 配置与依赖预加载（setImmediate/process.env 补丁） | 29 | `ensureBootstrapMacro()` |
| 3 | [`src/entrypoints/cli.tsx`](/src/src/entrypoints/cli.tsx.md) | 快速路径分发器：解析 process.argv，12+ 动态 import 分支，fallback 到 main.tsx | 303 | `cliMain()` |

### Supporting Files (utilities/helpers used by core)
| # | File | Role | Lines | Called By |
|---|------|------|-------|-----------|
| 1 | [`src/utils/earlyInput.ts`](/src/src/utils/earlyInput.ts.md) | stdin 早期捕获：在 Commander 接管前缓存 stdin pipe 数据 | 191 | cli.tsx |
| 2 | [`src/utils/startupProfiler.ts`](/src/src/utils/startupProfiler.ts.md) | 启动阶段性能埋点：profileCheckpoint/profileReport | 194 | cli.tsx |

## Branch Points (从 cli.tsx 分叉)

| File:Function | Branches To | Likely Main Line | Notes |
|--------------|-------------|-----------------|-------|
| cli.tsx:L160 `--version` | 内联 process.stdout.write | — | 单行输出，不构成 BL |
| cli.tsx:L168 `--dump-system-prompt` | config+model+prompts | ML-01-2 | &lt;3 文件，不构成 BL |
| cli.tsx:L176 `--claude-in-chrome-mcp` | `../mcpServer/claudeInChrome` | **ML-05** | MCP 服务，cross-ref |
| cli.tsx:L185 `--chrome-native-host` | `../mcpServer/claudeInChrome` | **ML-05** | Chrome 原生主机，cross-ref |
| cli.tsx:L193 `--computer-use-mcp` | `../mcpServer/computerUse` | **ML-05** | MCP 服务，cross-ref |
| cli.tsx:L206 `--daemon-worker` | daemon/workerRegistry | **ML-09** | daemon worker，cross-ref |
| cli.tsx:L191 `ps/logs/attach/kill/--bg` | `../cli/bg.js` | — | **源文件不存在**（bg.ts/bg.tsx 缺失） |
| cli.tsx:L216 `new/list/reply` | `../cli/handlers/templateJobs.js` | — | **源文件不存在** |
| cli.tsx:L230 `environment-runner` | `../environment-runner/main.js` | — | **目录为空**（0 .ts 文件） |
| cli.tsx:L242 `self-hosted-runner` | `../self-hosted-runner/main.js` | — | **目录为空**（0 .ts 文件） |
| cli.tsx:L253 `bridge/remote-control/rc/remote/sync` | `../bridge/bridgeMain.js` | **ML-09** | Bridge 主线，cross-ref |
| cli.tsx:L261 `daemon` | `../daemon/main.js` | — | **目录为空**（0 .ts 文件） |
| cli.tsx:L257 `--tmux --worktree` | worktreeModeEnabled/worktree | — | ≤2 文件，不构成 BL |

## Cross-References (shared with other main lines)
| File | This Main Line Role | Other Main Line(s) |
|------|-------------------|-------------------|
| [`src/entrypoints/cli.tsx`](/src/src/entrypoints/cli.tsx.md) | 快速路径分支入口 | ML-09 (bridge), ML-05 (mcp) |
| [`src/utils/startupProfiler.ts`](/src/src/utils/startupProfiler.ts.md) | 启动埋点 | ML-02 (query engine) |

## Branch Lines
无。所有有意义分支要么归其他 ML（ML-05/ML-09），要么源文件不存在。

## Pattern Instances
本段无 Pattern Catalog（PI-02 在 ML-01-3 段处理）。

## Segment Summary
- Total files traced: 5 (core: 3, supporting: 2)
- Total ML lines (DEEP): 722
- Branch lines discovered: 0
- Entry → Exit: `bootstrap-entry.ts` → `bootstrapMacro.ts` → `cli.tsx` → fast-path dispatch 或 fallthrough 到 main.tsx
- Key architectural insight: CLI 启动采用「绝对入口 → 宏配置 → 快速路径分发」三级架构，快速路径通过 dynamic import 实现按需加载，避免主包体积膨胀。大量 fast-path 目标文件在逆向还原的源码树中不存在（environment-runner/self-hosted-runner/daemon/bg/templateJobs），说明这些模块可能来自独立编译产物。
